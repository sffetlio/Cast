package cast.server.players;

import cast.server.rooms.BattleRoom;

/**
 * Server controlled AI client, similar to {@link Trainer} and used in {@link BattleRoom} with another {@link Client}.
 */
public class AIEnemy extends Player {
	
	public AIEnemy() {
		super();
	}

}
